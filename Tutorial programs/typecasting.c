#include<stdio.h>

void main()
{
   //program to print the ASCII value of the alphabets
   //capital case
   // using typecasting
   char ch='a';
   for(int i=1; i<=122; i++)
   {
    printf("ASCII value of %c is %d\n", (char)i , i);
   }
   for(int i=1; i<=12; i++){
printf("%c", (char)3);
printf("%c", (char)2);
   }
}